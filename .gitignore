# Python
__pycache__/
*.pyc
venv/
.venv/

# Media — never commit raw episodes or clips
input/*
output/*
!input/.gitkeep
!output/.gitkeep
*.mp4
*.mov

# macOS
.DS_Store
**/.DS_Store

# render/ — the Wave 2 (KH-MGX-001) Remotion project. node_modules is npm-installed,
# never committed; brand.json is generated by `python -m src.export_brand` from
# src/brand.py (the source of truth) — never hand-edited or committed either.
render/node_modules/
render/brand.json
